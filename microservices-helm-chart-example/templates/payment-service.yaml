apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment-service
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-payment-service
  ports:
    - port: 80
      targetPort: {{ .Values.paymentService.port }}
      nodePort: {{ .Values.paymentService.nodePort }}
